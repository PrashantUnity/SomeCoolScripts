## Enum Get Name

```cs
using System; 

Console.WriteLine(Enum.GetName(typeof(Language), Language.cs)); 
public enum Language
{
    csharp, 
    cs,
    dotnet,
    hlsl,
    gdscript,
    glsl
}
```
